import { DefaultTheme, Provider as PaperProvider } from 'react-native-paper';

export default  theme = {
    ...DefaultTheme,
    colors: {
      ...DefaultTheme.colors,
      blue: '#5DADE2',
      background : '#FFF',
      onSurface: '#6200ee'
    },
  };